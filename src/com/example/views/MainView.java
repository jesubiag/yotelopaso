package com.example.views;

import com.vaadin.navigator.View;

public interface MainView extends View {
	
	interface MainViewListener {
		//boolean redirectIfLogged(Double userId, boolean isCurrentView);
	}

}
